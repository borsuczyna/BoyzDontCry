export default class Camera {
    limits: [number, number] = [0, 0];
    x: number = 0;
    y: number = 0;

    // get x() {
    //     return this._x;
    // }

    // get y() {
    //     return this._y;
    // }

    // set x(value) {
    //     this._x = Math.max(Math.min(Math.max(this.limits[0] - 800, 0), value), 0);
    // }

    // set y(value) {
    //     this._y = Math.max(Math.min(Math.max(this.limits[1] - 600, 0), value), 0);
    // }
}